from django.apps import AppConfig


class EngagementConfig(AppConfig):
    """Configuration for the engagement domain."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "core.engagement"
    verbose_name = "Engagement"
