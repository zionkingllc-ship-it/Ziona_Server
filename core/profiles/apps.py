from django.apps import AppConfig


class ProfilesConfig(AppConfig):
    """Configuration for the profiles domain."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "core.profiles"
    verbose_name = "Profiles"
